server-context: primary

storage:
  mysql:
    use: true
    host: localhost
    port: 3306
    database: punishments
    username: root
    password: password

messaging:
  redis:
    use: true
    host: localhost
    port: 6379
  bungeecord:
    use: false

enable-ban-evade-warnings: true
ban-appeal-link: www.ruinscraft.com
